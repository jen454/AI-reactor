{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions granted to the AI reactor popover webview. `set-size` is needed because the panel sizes the window to its own content.",
  "windows": [
    "popover"
  ],
  "permissions": [
    "core:default",
    "core:window:allow-set-size"
  ]
}
